# sample_reviews/sample3.diff
--- a/helpers.py
+++ b/helpers.py
@@ -0,0 +1,6 @@
+def format_name(first, last):
+    return f"{first} {last}".strip()
+
+def is_valid_age(age):
+    return isinstance(age, int) and 0 <= age <= 120